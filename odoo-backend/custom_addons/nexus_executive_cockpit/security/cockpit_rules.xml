<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_nexus_cockpit_layout_own" model="ir.rule">
        <field name="name">Cockpit Layout: user sees only own layout</field>
        <field name="model_id" ref="model_nexus_cockpit_layout"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
