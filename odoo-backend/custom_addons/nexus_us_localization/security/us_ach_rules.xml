<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ACH payment wizard records are private to their creator. -->
    <record id="rule_nexus_us_ach_payment_owner" model="ir.rule">
        <field name="name">ACH Payment: only creator can access</field>
        <field name="model_id" ref="model_nexus_us_ach_payment"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- System administrators can see all ACH wizards for support/audit. -->
    <record id="rule_nexus_us_ach_payment_admin" model="ir.rule">
        <field name="name">ACH Payment: admins can access all</field>
        <field name="model_id" ref="model_nexus_us_ach_payment"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
