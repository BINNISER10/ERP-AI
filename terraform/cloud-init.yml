#cloud-config
package_update: true
package_upgrade: true

packages:
  - ca-certificates
  - curl
  - gnupg
  - lsb-release
  - git
  - ufw

runcmd:
  - |
    # Install Docker Engine and Compose plugin
    install -m 0755 -d /etc/apt/keyrings
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
    chmod a+r /etc/apt/keyrings/docker.gpg
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
    apt-get update
    apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
    systemctl start docker
    systemctl enable docker
  - |
    # Create nexus user and allow docker
    id -u nexus &>/dev/null || useradd -m -s /bin/bash nexus
    usermod -aG docker nexus
  - |
    # Allow ERP ports in UFW
    ufw allow 22/tcp
    ufw allow 8069/tcp
    ufw allow 8000/tcp
    ufw --force enable

final_message: "Nexus ERP server is ready. Please provide the SSH key to the operator to continue deployment."
